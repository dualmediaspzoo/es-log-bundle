{# @var value array #}
{% for item in value %}
    {% if isArrayOfBackedEnums %}
        {% include '@EsLog/field/enum.html.twig' with {value: item} only %}
    {% else %}
        {{ item }}
    {% endif %}
{% endfor %}